/**
 * <b>PROVISIONAL</b> extension API of the IPED RCP analysis UI.
 *
 * <p>
 * Contract: {@code specs/004-rcp-gui-migration/contracts/ui-extension-api.contract.md}.
 * Until formally declared stable (1-2 releases after the RCP cut-over), this
 * API may change <b>without a deprecation cycle</b>. Once stable it becomes
 * subject to Principle I of the project constitution (no silent removals or
 * renames).
 *
 * <p>
 * Third-party bundles must compile against this package only; everything else
 * in the RCP UI is internal and carries no compatibility guarantee.
 *
 * @apiNote Provisional API — subject to change without deprecation cycle.
 */
package iped.rcp.api;
